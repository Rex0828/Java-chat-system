package common.util;

/**
 * 全局常量。
 * 集中管理端口、超时时间、缓冲区大小等配置值，
 * 客户端与服务端引用同一份，避免两端硬编码不一致。
 */
public final class Constants {

    private Constants() {
        // 工具类，禁止实例化
    }

    // TODO: 第一阶段 — 定义 PORT / TIMEOUT / BUFFER_SIZE 等常量
}
